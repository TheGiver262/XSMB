# Live evaluation

Generated files:

- `daily_metrics.csv`: one row per settled live forecast.
- `rolling_summary.csv`: 30/60/90-draw live score summaries.
- `model_runs.csv`: model/backtest settings after each settled draw.

Historical backtests and live pre-committed forecasts are intentionally kept conceptually separate.
